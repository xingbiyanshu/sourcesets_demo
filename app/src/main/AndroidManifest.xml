<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sissi.sourcesetsdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"> <!--应用名称留给特定源集指定-->
<!--        <activity
            android:name=".WelcomeActivity"
            android:label="@string/title_activity_welcome">
            <intent-filter android:label="@string/app_name"> &lt;!&ndash;此处若不设定android:label则会导致app在桌面展示的名称为WelcomeActivity的名称&ndash;&gt;
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->
        <activity
            android:name=".WelcomeActivity">
            <intent-filter> <!--此处不再指定android:label，否则无法将应用名称留给特定源集指定-->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"/>

    </application>

</manifest>